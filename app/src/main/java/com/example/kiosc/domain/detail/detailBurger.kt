package com.example.kiosc.domain.detail

class detailBurger {
    fun menu1() {
        var name = "ShackBurger"
        var price = 6.9
        println("수량을 입력하여 주세요")
        var quantity =  readLine()!!.toInt()
        println("${name} | W ${price*quantity} | ${quantity}개 | 토마토, 양상추, 쉑소스가 토핑된 치즈버거")
    }

    fun menu2() {
        var name = "ShackBurger"
        var price = 6.9
        println("수량을 입력하여 주세요")
        var quantity =  readLine()!!.toInt()
        println("${name} | W ${price*quantity} | ${quantity}개 | 토마토, 양상추, 쉑소스가 토핑된 치즈버거")
    }

    fun menu3() {
        var name = "ShackBurger"
        var price = 6.9
        println("수량을 입력하여 주세요")
        var quantity =  readLine()!!.toInt()
        println("${name} | W ${price*quantity} | ${quantity}개 | 토마토, 양상추, 쉑소스가 토핑된 치즈버거")
    }

    fun menu4() {
        var name = "ShackBurger"
        var price = 6.9
        println("수량을 입력하여 주세요")
        var quantity =  readLine()!!.toInt()
        println("${name} | W ${price*quantity} | ${quantity}개 | 토마토, 양상추, 쉑소스가 토핑된 치즈버거")
    }

    fun menu5() {
        var name = "ShackBurger"
        var price = 6.9
        println("수량을 입력하여 주세요")
        var quantity =  readLine()!!.toInt()
        println("${name} | W ${price*quantity} | ${quantity}개 | 토마토, 양상추, 쉑소스가 토핑된 치즈버거")
    }
}